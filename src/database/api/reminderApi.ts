export const createReminder = (discordUserId: string, reminderMessage: string, time: Date) => {
  // const reminder = new Reminder();
  // reminder.userId = discordUserId;
  // reminder.reminder = reminderMessage;
  // reminder.time = time;
  // im guessing flow here should be
  // check for user,
  // if user doesn't exist, create it
  // once user exists, add reminder for that user
  // need to think about handling weather, when it should create user enitity, etc
};
